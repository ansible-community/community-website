<div class="grid-wrapper homepage-tab-menu-band full-width-band">
  <div class="tab-menu-row width-12-12 width-12-12-m">
    <!-- Ansible section -->
    <a class="tab-menu-anchor" href="#ansible" role="button">
      <img src="{{ homepage.ansible.section_icon }}"
           alt="{{ homepage.ansible.icon_alt }}"
           width="40"
           height="40" />
      <p>{{ homepage.ansible.title }}</p>
    </a>
    <!-- Catalyst section -->
    <a class="tab-menu-anchor" href="#catalyst" role="button">
      <img src="{{ homepage.catalyst.section_icon }}"
           alt="{{ homepage.catalyst.icon_alt }}"
           width="40"
           height="40" />
      <p>{{ homepage.catalyst.title }}</p>
    </a>
    <!-- Ecosystem section -->
    <a class="tab-menu-anchor" href="#ecosystem" role="button">
      <img src="{{ homepage.ecosystem.section_icon }}"
           alt="{{ homepage.ecosystem.icon_alt }}"
           width="40"
           height="40" />
      <p>{{ homepage.ecosystem.title }}</p>
    </a>
    <!-- Community section -->
    <a class="tab-menu-anchor" href="#community" role="button">
      <img src="{{ homepage.community.section_icon }}"
           alt="{{ homepage.community.icon_alt }}"
           width="40"
           height="40" />
      <p>{{ homepage.community.title }}</p>
    </a>
    <!-- Events section -->
    <a class="tab-menu-anchor" href="https://forum.ansible.com/c/events/8" target="_blank" role="button">
      <img src="{{ homepage.events.section_icon }}"
           alt="{{ homepage.events.icon_alt }}"
           width="40"
           height="40" />
      <p>{{ homepage.events.title }}</p>
    </a>
  </div>
</div>
